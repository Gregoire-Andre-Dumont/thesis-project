# Linux/RunPod dependency lock — pinned to match the working Windows venv (Python 3.12).
# Install into a venv on the pod:  pip install -r requirements-runpod.txt
# torch/torchvision pinned to CUDA 12.6 wheels; use a CUDA >=12.6 pod.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.1+cu126
torchvision==0.27.1+cu126

numpy==2.5.0
scipy==1.18.0
scikit-learn==1.9.0
# headless build avoids libGL errors on a server (cv2 is used only for array ops, no GUI)
opencv-python-headless==4.13.0.92
decord==0.6.0
einops==0.8.2
iopath==0.1.10
pillow==12.2.0
matplotlib==3.11.0
hydra-core==1.3.3
omegaconf==2.3.1
tqdm==4.68.3
epochalyst==4.1.0

# SAM 2 — the SAMITE fork (provides `sam2` package used by samite/samurai/sam2long wrappers)
git+https://github.com/Sam1224/SAMITE.git@b1b1d1918c08904b786da77f10663da04da4edc6#subdirectory=sam2
