tracker:
  _target_: src.modules.baselines.sentry.Sentry
  checkpoint: tm/sam_large.pt
  model_config: configs/sam2/sam2_hiera_l.yaml   
  sentry_root: /workspace/SENTRY                  
  policy: default  
  device: cuda:0

experiment:
  _target_: src.experiments.tracking_experiment.TrackingExperiment
  resolution: 1024
  experiment_name: sentry
  video_path: data/visualize/sentry
