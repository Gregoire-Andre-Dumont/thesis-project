defaults:
  - offline_trainers: experiment_2/cnn_fixed_samara_large
  - trackers@tracker: memory_oracle
  
detection_data:
  _target_: src.typing.detection_data.DetectionData
  amodal_directory: data/person_path/amodal
  visible_directory: data/person_path/visible
  video_directory: data/person_path/videos
  load_frames: True
  num_threads: 32

train_test_split: 0.20
deploy_controller: True
dataset_path: data/memory_oracle_large/memory
commit_threshold: 0.4

# run_experiment.py selectors (override on CLI, e.g. experiment=5 size=tiny max_test_trajectories=20)
experiment: 1
size: null
tracker_name: null
max_test_trajectories: null