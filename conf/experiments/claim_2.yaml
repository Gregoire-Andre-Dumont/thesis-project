# @package _global_
detection_data:
  _target_: src.typing.detection_data.DetectionData
  amodal_directory: data/person_path/amodal
  visible_directory: data/person_path/visible
  video_directory: data/person_path/videos
  load_frames: True
  num_threads: 32

person_path:
  _target_: src.typing.person_path.PersonPath
  main_directory: data/person_path
  n_experiments: 1200
  random_seed: 44
  first_occ_min: 4
  border_inset: 8  
  n_after_occlusion: 30 
  min_visible_area: 1024
  min_visible_ratio: 0.90
  occlusion_ranges: [5, 75] 
  max_distractor_overlap: 0.40 
  non_targets: [crowd, person_in_vehicle, reflection, person_in_background, severly_occluded_person, sitting_person, person_on_open_vehicle]

# experiment parameters
n_bins: 4
n_traj: 400
commit_threshold: 0.2 
corruption_ps: [0.0, 0.05, 0.1, 0.15, 0.2]
max_frames: 300
out_dir: data/claim_2
oracle_config: conf/trackers/baselines/memory_oracle.yaml
mask_oracle_config: conf/trackers/baselines/mask_oracle.yaml
